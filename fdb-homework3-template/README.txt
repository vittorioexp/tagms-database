In the "Code" folder there are both the SQL and JAVA files. The SQL files are about the database creation, population and principal queries. The java classes implement some principal queries.

In the "HW3_Latex" folder there are the .tex files and the compiled pdf of the report.

In the "tagms-webapp.zip" archive, there is a functioning Java Enterprise project, created with IntelliJ Idea, used to test the java files.